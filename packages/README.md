# Packages

This directory is reserved for published, UI-independent TypeScript domain interfaces and server modules. It intentionally contains no package implementation during the repository-foundation migration.
