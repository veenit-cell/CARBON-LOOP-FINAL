# @carbonloop/factor-registry

Versioned emission-factor contracts for CarbonLoop. This Stage 4A.1 scaffold defines factor metadata and validation only; it does not contain real emission factors, selection, approval workflows, or calculations.

`quantityUnit` and `factorUnit` follow the current shared-schema unit contract: non-empty strings. A canonical unit catalogue has not yet been introduced in `@carbonloop/schemas`.
